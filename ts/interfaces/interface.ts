export interface InputList {
  firstName: string;
  lastName: string;
}

export interface Todo {
  id: number;
  description: string;
  isComplete: boolean;
}

export interface Language {
  locale: string;
  label: string;
}
